const vec<int> v = vec[0];

class Test {
    const vec<int> CLASS_CONST = vec[0];
}

function foo(vec<int> $v = vec[0]): void {}

const dict<string, (function(Webserver\RequestURL): Awaitable<void>)> CONSTANT_NAME = dict[];
